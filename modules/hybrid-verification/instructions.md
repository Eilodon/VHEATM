# Hybrid verification

For every mandatory finding, rate static confidence, choose a dynamic confirmation that can falsify the claim, execute it when permitted, and reconcile the result. LLM triage may prioritize candidates but cannot replace direct confirmation.

Record unavailable tools and preserve `unknown`; never convert skipped execution into a pass. A mandatory claim lacks closure until static and dynamic evidence are reconciled or explicitly unavailable.
