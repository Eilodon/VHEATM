schema_version: 1.0.0
id: MOD-CONTEXT-CONTRACT
version: 0.2.0
status: experimental
kind: core
title: Context contract
summary: Validates scope, declarations, evidence currency, ownership, and fail-safe unknown handling.
gate_coverage:
- HG-P
phase_coverage:
- P
selection:
  policy: any_active_gate
  priority: 10
  dependencies: []
  conflicts: []
contract:
  inputs:
    required_context_fields:
    - mode
    - target_tier
    artifact_classes:
    - repository
    - audit_context
  outputs:
  - id: module_decision
    required: true
    description: Decision-bearing module result with gate trace and evidence references.
    schema_ref: https://vheatm.dev/schemas/module-decision.schema.json
    cardinality: one
    required_when: always
  evidence:
    required_kinds:
    - document
    - code
    - test
    minimum_sources: 1
    freshness_required: true
  failure:
    on_missing_input: block
    on_tool_denied: block
    produces_unknown: true
  provenance:
    claims_required: true
    sources_required: true
    legacy_refs:
    - VHEATM-v16.1.1.skill::SKILL.md#Entry Protocol — Minimum 3 Fields, Maximum 13
    - VHEATM-v16.1.1.skill::references/00-context-modes.md#Context Mode — Self-Check
  runtime:
    tool_classes:
    - read
    sandbox_required: false
    network_required: false
  disclosure:
    router_summary: Validates scope, declarations, evidence currency, ownership, and fail-safe unknown
      handling.
    instruction_path: instructions.md
    instruction_sha256: 29b21639a3ef225f62dad5a5f30ccb8f4bd9226bdd624a5ff3b88ab327d046a3
    token_budget: 512
