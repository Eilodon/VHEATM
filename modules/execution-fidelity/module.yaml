schema_version: 1.0.0
id: MOD-EXECUTION-FIDELITY
version: 0.1.0
status: experimental
kind: meta
title: Execution fidelity
summary: Reconciles selected modules, outputs, evidence, findings, lifecycle, and
  gate results before completion.
gate_coverage:
- HG-EF
phase_coverage:
- M
selection:
  policy: any_active_gate
  priority: 900
  dependencies:
  - MOD-CONTEXT-CONTRACT
  conflicts: []
contract:
  inputs:
    required_context_fields: []
    artifact_classes:
    - repository
    - audit_context
  outputs:
  - id: module_decision
    required: true
    description: Decision-bearing module result with gate trace and evidence references.
  evidence:
    required_kinds:
    - document
    - code
    - test
    minimum_sources: 1
    freshness_required: true
  failure:
    on_missing_input: block
    on_tool_denied: block
    produces_unknown: true
  provenance:
    claims_required: true
    sources_required: true
    legacy_refs:
    - v16.1.1 HG-EF execution fidelity meta-defense
  runtime:
    tool_classes:
    - read
    sandbox_required: false
    network_required: false
  disclosure:
    router_summary: Reconciles selected modules, outputs, evidence, findings, lifecycle,
      and gate results before completion.
    instruction_path: instructions.md
    instruction_sha256: 9293319d8ca343185580c6611f9933cd8e3baa866567865a6a405e13c2f9abd9
    token_budget: 512
