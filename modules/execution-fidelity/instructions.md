# Execution fidelity

Reconcile the activation plan, selected modules, produced outputs, evidence references, gate results, findings, and lifecycle events. Every selected module must produce its required decision-bearing outputs or an explicit contract-compliant failure result.

Detect omitted sections, duplicate or stale evidence, unsupported pass states, skipped dependencies, and substitutions that weaken the declared method. Do not accept narrative assertions that are absent from machine records.

Pass `HG-EF` only when the execution trace demonstrates complete contract coverage for the current plan and all unresolved work remains visible as blocking or partial state.
